#!/bin/sh
# pre-workflow.sh — runtime pre-workflow hook (self-contained, no lib sourcing)
# Central context pull implemented in Epic 5; stub exits 0 for Epic 1.

printf '[INFO] pre-workflow.sh: stub (context pull in Epic 5)\n' >&2
exit 0
